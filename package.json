{
  "name": "scraping-base",
  "version": "1.0.0",
  "description": "HTTP service that scrapes URLs through a rotational DC proxy, stores pages in MongoDB, and logs every request.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "scrape": "node scripts/scrape.js"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "cheerio": "^1.0.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "https-proxy-agent": "^7.0.5",
    "mongoose": "^8.7.0",
    "winston": "^3.14.2"
  }
}
